import React from 'react';

export const InquireComments = ({  }) => {

  return (
    <div className="paging-number">
      {pageNumbers.map(number => (
        <div>
          
        </div>
      ))}
    </div>
  )
}

export default InquireComments;

import React from 'react';
import BlogItems from '../../components/BlogContents/BlogItems';

const Blog = props => {
  return (
    <div className="all">
      <BlogItems />
    </div>
  );
};

export default Blog;